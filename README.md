# Task Manager App

Simple fullstack app: React + Node.js + MySQL.

## Import SQL file

```bash
mysql -u root < tasks.sql
```

## Setup

```bash
cd client
npm install
npm run build
cd server
npm install
node server.js
```